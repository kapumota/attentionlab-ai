### A3.1 Actualización de bibliografía

#### Objetivo

Actualizar `referencias_attention.bib` antes de modificar el manuscrito, usando como base las decisiones científicas cerradas en A2.4 a A2.7.

A3.1 no modifica `lara_avila.tex`, no modifica `v1.2.0`, no introduce CUDA y no introduce `physical_calibration`.

#### Archivo fuente real

El paquete LaTeX vigente usa:

```text
referencias_attention.bib
```

Por tanto, A3.1 conserva ese nombre. El nombre conceptual `references.bib` puede reservarse para una reorganización posterior del artefacto, pero no es necesario renombrarlo durante esta subfase.

#### Criterios aplicados

1. Conservar todas las claves BibTeX ya citadas por el manuscrito para evitar romper referencias existentes.
2. Reemplazar metadatos provisionales por metadatos formales cuando existe una publicación estable, especialmente GQA, PagedAttention y KIVI.
3. Incorporar el conjunto mínimo definido en A2.7 para arquitectura, compression, quantization, serving, logical versus physical y analytical positioning.
4. Incorporar XKV y The Pitfalls of KV Cache Compression porque A2 los identifica como referencias importantes para budget semantics y amenazas de calidad.
5. Mantener `lara2026attentionlab` apuntando a `v1.2.0` durante A3.1. El nuevo release no debe citarse antes de existir.
6. No insertar todavía nuevas `\cite{}` en el manuscrito. Esa tarea pertenece a A3.2 y A3.3.

#### Entradas nuevas

Se agregaron 14 claves:

```text
brandon2024crosslayer
chang2025palu
zhang2023h2o
xiao2024streamingllm
xiao2025duoattention
duanmu2024skvq
prabhu2025vattention
rehg2024kvcompress
gao2025rethinking
patwari2025life
gokhale2026kvpareto
li2024xkv
li2024kvsurvey
chen2026pitfalls
```

El archivo pasa de 10 a 24 entradas. Esto no implica que las 24 deban aparecer en la bibliografía final. BibTeX solo imprimirá las referencias citadas. A2.7 recomienda mantener la versión final aproximadamente entre 18 y 22 referencias impresas.

#### Referencias formalizadas

Se conservaron las claves existentes y se mejoraron sus metadatos cuando había una publicación formal:

```text
ainslie2023gqa
kwon2023efficient
liu2024kivi
```

GQA queda vinculado a EMNLP 2023 y su DOI.

PagedAttention queda vinculado a SOSP 2023 y su DOI.

KIVI queda vinculado a ICML 2024, PMLR 235.

#### Decisión sobre Attention AI Lab

Durante A3.1 se mantiene:

```text
Version 1.2.0
commit 5e24e362302a14f8d8d3d77e56e9f964c2037ae1
```

No se inventa ni se anticipa `v1.3.0-paper-revision`. En A3.8, después del freeze y de crear el tag, se decidirá cómo citar el artefacto revisado y cómo conservar la trazabilidad con la versión originalmente sometida.

#### Validación ejecutada

Se verificó:

```text
24 entradas BibTeX
24 claves únicas
0 claves duplicadas
0 claves actualmente citadas ausentes
compilación LaTeX + BibTeX Unicode correcta
0 citas indefinidas después de recompilar
7 páginas en el manuscrito sin cambios de contenido
10 referencias impresas actualmente
```

Las 10 referencias impresas son las que ya cita `lara_avila.tex`. Las nuevas entradas comenzarán a aparecer cuando A3.2 y A3.3 incorporen las citas correspondientes.

#### Comando de validación usado

```bash
pdflatex -interaction=nonstopmode -halt-on-error lara_avila.tex
bibtexu lara_avila
pdflatex -interaction=nonstopmode -halt-on-error lara_avila.tex
pdflatex -interaction=nonstopmode -halt-on-error lara_avila.tex
```

Se usó `bibtexu` porque la bibliografía contiene caracteres Unicode en el título del repositorio y esta variante preserva correctamente la codificación durante la validación.

#### Criterio de cierre de A3.1

A3.1 puede considerarse cerrada cuando el archivo actualizado sustituya a `referencias_attention.bib` en la rama de revisión, se ejecute la compilación anterior y se confirme que no existen citas indefinidas ni errores BibTeX.

No hace falta editar todavía el cuerpo del paper. El siguiente paso es A3.2, integrar Related Work y reformular las contribuciones usando las claves ya disponibles.
