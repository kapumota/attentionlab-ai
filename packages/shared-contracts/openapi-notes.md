# Contratos compartidos

En v0.5 los contratos viven en `apps/api/app/schemas/contracts.py` y el OpenAPI se expone automáticamente en `/docs` y `/openapi.json`.

Para una v0.6 se recomienda generar clientes TypeScript desde `/openapi.json` y colocarlos en este paquete.
